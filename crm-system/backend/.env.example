# Backend Configuration
DATABASE_URL="postgresql://postgres:prisma@postgres:5432/postgres?schema=public"
BACKEND_PORT=3001

# Environment
NODE_ENV=development

# JWT Secret (use a local value and never commit real secrets)
JWT_SECRET=change-this-secret-locally
# Backend
BACKEND_PORT=3001
DATABASE_URL="postgresql://crm_user:change-me@localhost:5432/crm_database?schema=public"
NODE_ENV=development
JWT_SECRET="change-this-secret"